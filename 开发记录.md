# 开发记录

> 面向协作者的实施日志：每轮开发记录"做了什么 / 怎么跑 / 决策与偏差 / 下一步"。
> 文档基线：`策划案.md` V2.1 + `技术方案.md` V2.1（审核报告 A/B/C 类问题已在该两份 V2.1 中修正）。

---

## 2026-09-10 · P1 战斗核心交付（3 角色 / 技能 / 等价交换 / 精英）

- **范围**（技术方案 §11 P1）：3 角色常驻与切换（艾登·金近战 / 莉赛尔·火远程 / 卡文·水辅助）、六技能（E 冷却 + Q 能量，形态：直线/环绕/扇形/投掷火域/治疗环/冻结）、等价交换 R（血/智/质三代价 + 强化）、MP·能量·炼金尘资源规则、蚀化猎犬/锈蚀傀儡/游荡水元素/精英·矿监工（霸体条 + 破霸易伤 + 双阶段 + 召唤）、HUD 战斗件（MP/能量/技能 CD/队伍栏/R 渐晕/掉落计数）。
- **自查闭环**（用户流程：检查→修复→再检查）：两轮共修复 8 个问题，最重的是 `PartyManager.setup` 未覆盖 `member.stats`（三角色全为艾登）、`CharacterRuntime.hp` 字段缺失（切换崩溃）、`main.gd` 先 setup 后 add_child（装配期空引用）。详见 `代码审核报告.md` 第一部分。
- **验证**：单元 118/118、集成 43/43（新增 29 项 P1 用例）、主场景冒烟 0 脚本错误。
- **设计取舍**：精英 level 18（对齐 60-90s 验收，玩家练度上来后回正）；质祭效果时长 ×2 对持续型从宽；卡文治疗 12% 最大HP×(1+EM/400)；傀儡/猎犬伤害倍率 1.3×/0.8×；削霸数值普攻 5/5/8、E 20、Q 40 均为首版可调项。
- **P0 遗留清理**：Z/X/C 调试元素脚手架按计划移除（被真实三角色取代）。

---

## 2026-09-10 · 玩测反馈第三轮：沙盒敌人血量

- **反馈**（用户自查定位）：看不到反应的真正原因是怪血太少——教学怪 33 HP 一套连击就死，反应链来不及表演。
- **处置（主策划决策）**：不动教学怪正式数值（33 HP 是策划案 9.3 的文档锚点、有测试锁定）；竞技场作为沙盒给敌人加 `hp_multiplier = 8`（33 → 264 HP，DEF/攻击不变），一场战斗足够打完"金附着→切火→熔金→破甲→锈蚀 DoT"全链。正式关卡不使用此字段。
- **回归**：单元 87/87、集成 24/24（新增沙盒血量断言 264）。

---

## 2026-09-10 · 玩测反馈第二轮：竞技场敌人补刷

- **反馈**：打死一波怪之后不刷新。
- **定性**：白模竞技场原本一次性布设 4 只敌人、无刷新逻辑（属范围缺口而非代码 bug），但对持续试玩是硬伤。
- **修改**：`p0_arena.gd` 增加槽位化补刷——敌人死亡 6 秒后原地补刷一只，全场恒定 4 只；`K` 清怪后 6 秒同样回满。此逻辑为白模竞技场专属，正式玩法的怪物刷新规则（精英/BOSS/采集刷新）按策划案 8.1 在 P2 内容期另行设计。集成测试 +3 项（初始 4 只 / 击杀清零 / 补刷回满），回归全绿（单元 87/87、集成 23/23）。

---

## 2026-09-10 · 玩测反馈第一轮：战斗反馈可视化

- **反馈**：移动/跳跃手感 OK；①伤害数字太小不显眼；②看不到元素附着，怀疑系统不存在。
- **结论**：附着系统在工作（集成测试可证），是**呈现层缺口**——白模无视觉表现 + P0 玩家只有金元素可打，正常游玩触发不了反应。
- **修改**：伤害数字世界尺寸 0.16m → 0.46m（暴击 0.64m）、加粗描边、入场弹跳、滞留加长；新增 `AuraVisualizer`（宿主身体随附着元素染色、头顶元素色光珠随 GU 脉动）；反应触发时头顶弹出反应名（紫色 Label3D，走伤害数字池）；调试元素脚手架 `Z/X/C`（火/水/金，仅 debug build，P1 三角色实装后移除）+ HUD 覆盖提示。
- **回归**：单元 87/87、集成 20/20、零 FAIL。

---

## 2026-09-10 · 第一轮代码审核：10 项问题修复 + 测试扩容

- **报告**：`代码审核报告.md`（问题清单、根因、修复、验证、挂账事项）。
- **修复要点**：玩家受击击退被 HitState 覆盖归零（#1）；`enemy_died` 信号从未发射、移至 `EnemyBase._on_died` 单一来源覆盖 DoT 击杀（#2）；连段缓冲扩展到前摇段（#3）；对象池悬挂节点自愈（#4）；场景计时器改子节点 Timer（#5）；hitstop 恢复进入前缩放（#6）；存档校验和（#7）；本地化 fallback zh_CN（#8）；敌人互相碰撞（#9）；变量遮蔽（#10）。
- **测试**：单元 76 → **87**（+本地化/池/防篡改），集成 7 → **20**（+反应流经 Combat 精确数值 125/148、ICD、enemy_died 双路径、hitstop 恢复）。全部绿灯。
- **新踩坑**：对已释放实例，赋给类型化变量、`is` 运算都会报错，只有 `is_instance_valid()` 安全；JSON 往返把 int 变 float，存档校验和必须对文本做 hash 而非对解析后的结构。

---

## 2026-09-09 · P0 手感原型（代码骨架 + 数值核心）首轮实现

### 引擎与工具链

- **锁定 Godot 4.4.1-stable**（win64）。编辑器/命令行二进制放在仓库外：`F:\tools\godot-4.4.1\`（`.gitignore` 已排除 `*.exe`）。
- 渲染 Forward+、物理 **Jolt**（`project.godot` → `physics/3d/physics_engine`），均按技术方案 §1。

### 本轮完成范围

**技术方案 §11 P0 手感原型（全部验收项）+ 数值/规则核心（P1 的可测试地基）：**

| 模块 | 内容 | 对应文档 |
|------|------|---------|
| 角色控制器 | CharacterBody3D + 自实现重力；**土狼时间 0.1s / 跳跃缓冲 0.15s / 相机相对移动**；走/冲刺（体力 15/s 消耗、停 2s 后 20/s 回复） | 技术 §3.1 / 策划 §5.2 |
| 相机 | SpringArm3D 越肩相机，碰撞收缩防穿墙；灵敏度/臂长/FOV 全部 `camera_config.tres` | 技术 §3.1 |
| 白模战斗 | 三段连击（帧数据：windup/active/recover + 连段缓冲排队），第三段附 **1GU 金附着**；瞬时判定用 `intersect_shape()`（非 Area3D 信号） | 策划 §5.1/§7.1 / 技术 §4.1 |
| 命中反馈 | **顿帧（hitstop）/ 闪白（material_overlay）/ 击退 / 池化伤害数字**，参数在 `combat_config.tres` | P0 验收"顿帧/闪白/击退" |
| 敌人 AI | 蚀化矿工白模：IDLE→CHASE→WINDUP（膨胀前摇提示）→打击→RECOVER→冷却；受击进入硬直 | 技术 §4.4 / 策划 §5.4 |
| 数值公式 | `DamageFormulas` 纯函数：防御减免（上限 70%）、抗性、暴击期望、剧变伤害、成长曲线、敌人 HP 公式 | 策划 §9.2/§9.3 |
| 元素系统 | `ElementAura` 组件：GU 附着/取 max 不叠加/-0.2GU·s 衰减/**单一 ICD 2.5s**；`ReactionTable.tres` 数据驱动 5 反应（含白雾减速/熔金破甲/锈蚀 DoT/淬火硬直强化，`StatusHolder` 承接） | 策划 §5.3/§9.4 / 技术 §4.2 |
| 工程基建 | 6 个 Autoload（EventBus/GameManager/DataManager/SaveManager/AudioManager/PoolManager）；InputMap 全动作化（代码零硬编码键位）；本地化 `data/loc/zh.csv`；F3 调试面板（FPS/DrawCall/显存/时间缩放/无敌/传送/清怪/附着可视化）；占位灰模材质 | 技术 §2.1/§9/§10.2 |
| 联机预留 | `IActorSource` 输入抽象（本地实现 LocalActorSource），未写任何网络代码 | 技术 §12 |

### 如何运行 / 测试

```bat
:: 运行游戏（P0 竞技场）
F:\tools\godot-4.4.1\Godot_v4.4.1-stable_win64_console.exe --path F:\tools\GL

:: 单元/规则测试（76 项：公式锚点、5 反应、附着/ICD、状态、配置与场景可加载性、存档往返）
F:\tools\godot-4.4.1\Godot_v4.4.1-stable_win64_console.exe --headless --path F:\tools\GL res://tests/test_runner.tscn

:: 端到端集成测试（7 项：模拟真实输入驱动移动/三段连击/金附着/敌人追击与攻击）
F:\tools\godot-4.4.1\Godot_v4.4.1-stable_win64_console.exe --headless --path F:\tools\GL res://tests/integration_runner.tscn
```

操作：WASD 移动 / 鼠标视角 / 左键三段攻击 / Shift 冲刺 / 空格跳跃 / F3 调试 / Esc 暂停。
竞技场内容：3 个训练假人（连段与反应验证）、4 只蚀化矿工、20° 缓坡（可行走）与 70° 陡壁（P2 攀爬目标）。

### 与文档的偏差（及理由）

1. **敌人寻路暂用直线追踪**（技术方案 §4.4 写 NavigationAgent3D）——白模平地无障碍，导航烘焙放到 P1 地形就位后接入，接口已预留。
2. **附着消耗量（aura_cost=1.0GU）为首版可调常量**——策划案 §5.3 只定义了"按倍率扣除"，具体数值文档未给；已做成 `ReactionDefinition.aura_cost` 数据字段，配合测试随时校准。
3. **玩家 DEF 暂为 0**——策划案 §9.1 未定义玩家防御成长，P1 养成落地时补。
4. **白模动画/特效用代码表现**（前摇膨胀、闪白、顿帧）——Mixamo 动画流是 P1 资产项。
5. **存档只提供读写骨架**（schema_version=1，JSON 往返有测试）——party/world 完整映射按技术方案 §7 放 P3。

### 踩坑记录（协作者必读）

- **GDScript 4.4 解析器限制**：跨行字典字面量的首键不能用 `&"..."`（StringName 字面量）会报 "Expected expression as dictionary key"；单行没问题。 workaround：跨行字典用 String 键（运行时 String 与 StringName 作为字典键可互换，已验证）。
- **class_name 循环依赖**：`EnemyBase ⇄ DebugPanel` 互相引用会双双编译失败（Identifier not declared）。解法：低频开关走 `SceneTree` 元数据（`gl_show_aura_viz`）广播，不引用对方类。
- **`-s script.gd` 模式下 Autoload 不存在**：引用 `EventBus` 等单例的脚本在该模式下无法编译，所以测试用**场景式运行器**（`res://tests/*.tscn` 作为启动场景跑）而不是 `-s`。
- 反应伤害紫色调、元素色（火橙/水青/金铜）集中在 `Elements.color()`——特效复用重着色（技术方案 §6.3）。

### P0 验收对照（技术方案 §11）

| 验收项 | 状态 |
|--------|------|
| 角色控制器（移动/跳/冲刺） | ✅ 已实现 + 集成测试（移动位移 2.71m/40帧） |
| SpringArm 相机无穿墙抖动 | ✅ 已实现（mask 仅 world 层 + margin 0.25）；**"30 分钟连玩不烦躁"需真人手测** |
| 白模三段攻击与受击 | ✅ 集成测试：3 段命中、伤害 45.1（基准 44.5）、第三段金附着 1GU |
| 1 个敌人追击 AI | ✅ 集成测试：7m→1.3m 追击、命中玩家（30/45 暴击伤害） |
| 命中反馈明确（顿帧/闪白/击退） | ✅ 已实现，参数 `combat_config.tres` 可调 |

> ⚠️ 排期铁律（策划案 §11.3）：**P0 手感不达标（自测连玩 30 分钟不烦躁）不进入 P1**。代码侧已就绪，手感结论待所有者实机试玩。

### 下一步（P1 战斗核心，按技术方案 §11）

1. 3 角色常驻 + `PartyManager` 切换（1s 公共冷却、资源不重置、非激活者关碰撞层——技术方案 §3.2/R9）
2. 技能系统：E/Q 技能组（SkillData Resource）+ 能量获取规则（普攻+4/技能+8/受击+5/入场+10）
3. 等价交换 R 键：3 类代价（血/智/质）+ `EquivalenceComponent`（HP 保底 1 点不死）
4. 4 小怪 + 精英 AI（霸体条）、NavigationAgent3D 接入
5. AnimationTree 状态机接 Mixamo 动画（每角色约 19 条清单）
