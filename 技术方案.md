# 《等价之理：哲学家的旷野》Godot 技术方案

- 文档版本：V2.1（替代记录.txt 中全部旧版技术规划）
- 审核背景：本方案针对审核报告（见 `审核报告.md`）中的 C 类问题逐项修正
- 对应策划：`策划案.md` V2.1（四期路线）

## 修订记录

| 版本 | 日期 | 内容 |
|------|------|------|
| V2.0 | 2026-09-09 | 初版：选型纠偏、架构、性能预算、工程基础设施 |
| V2.1 | 2026-09-09 | ① 修正编号引用错误（动画资产对应 C5 而非 C5/C6；第 5 章涵盖 C2/C6/C7）；② 补充非激活角色的碰撞禁用（process_mode 禁用不阻止 Area 信号，须同时关碰撞层）；③ 攀爬角度表述与策划案统一为 ≥65°；④ 瞬时攻击判定补充 PhysicsShapeQuery 建议（帧同步精确性）；⑤ 存档 schema_version 示例从 3 改为 1（首版从 1 起）；⑥ 攀爬动画条目注明简化取舍 |

---

## 1. 技术选型总览（修正 C1/C2/C3）

| 层面 | 选型 | 关键修正说明 |
|------|------|-------------|
| 引擎 | **Godot 4.4+**（锁定具体小版本，全团队/全构建统一） | 旧方案"4.x 稳定版"过于含糊；版本不锁定会导致导出模板与 API 差异 |
| 语言 | **GDScript**（主） | 个人开发者迭代速度优先；热点性能代码后续可局部移 C# 或 GDExtension，不预选 |
| 渲染管线 | **PC 首发：Forward+**；若需覆盖老旧核显再评估 Compatibility | **修正**：Forward+ 不支持移动端；移动端（远期）必须用 Mobile/Compatibility 渲染器并重新验证光照方案 |
| 地形 | 第一期：**Blender 雕刻整块网格地形**；第二期：**Terrain3D 社区插件** | **修正**：GridMap 是体素网格仅适合室内/洞穴；TileMap 是 2D 节点。Godot **没有内置地形系统**，此为最关键外部依赖 |
| 物理 | Godot 内置物理（Jolt 选项开启：`physics/engine` 选择 Jolt Physics，4.4+ 随附） | 大量刚体场景下 Jolt 更稳定；CharacterBody3D 无缝切换 |
| 动画 | AnimationPlayer + AnimationTree（状态机）；骨骼动画经**统一骨架 + 重定向** | 修正 C5：动画是最大资产成本项，见第 8 章 |
| 网络 | **远期**；第一期仅做输入/状态抽象层（见第 12 章） | 修正：ENet 无 NAT 穿透，公网联机需中继（Steam/EOS），成本未验证前不投入 |
| 数据 | 静态配置 = Resource（只读）；运行时/存档 = JSON | 修正 C4：杜绝 Resource 运行时写回污染 |
| 版本管理 | Git + **Git LFS**（美术资产必走 LFS） | 修正 C9 |

---

## 2. 全局架构

### 2.1 Autoload 单例（全局唯一，按依赖顺序注册）

| 单例 | 职责 |
|------|------|
| `GameManager` | 游戏状态机（BOOT / MENU / PLAYING / PAUSED），场景切换 |
| `DataManager` | 加载全部静态配置 Resource（技能/物品/敌人/任务表），构建只读索引 |
| `SaveManager` | JSON 存档读写、schema 版本迁移、多存档位、自动存档 |
| `EventBus` | 全局信号总线（解耦：战斗事件、任务事件、UI 事件） |
| `AudioManager` | AudioBus 混音、BGM 切换、音效池 |
| `PoolManager` | 常用节点池（投射物、命中特效、伤害数字） |

> 场景级管理器（`PartyManager`、`WorldManager`、敌人 AI 管理等）挂在主游戏场景下，**不设为 Autoload**——它们依赖场景实例，全局化反而制造生命周期问题。

### 2.2 场景结构

```
res://
├── scenes/
│   ├── main/               # Main.tscn（启动入口）、Boot、MainMenu
│   ├── world/              # 灰烬河谷区域场景（第一期单场景）
│   │   └── ashvalley/
│   ├── characters/         # player/ 3角色、enemies/
│   ├── ui/                 # 13屏UI（对应策划案第10章）
│   └── common/             # 通用组件（伤害数字、拾取物、传送阵）
├── scripts/
│   ├── player/  enemies/  systems/  ui/  debug/
├── resources/              # 静态配置 .tres（技能/物品/敌人/任务/反应表）
├── data/                   # 存档 schema、本地化 CSV
├── assets/
│   ├── models/  animations/  textures/  materials/
│   ├── audio/  shaders/  fonts/
├── addons/                 # Terrain3D（第二期）、调试工具
└── tests/                  # 系统测试场景（反应触发、存档往返）
```

### 2.3 核心数据流

```
输入 → CharacterController（状态机） → 技能系统（读 SkillData）
  → 命中判定（Area3D/ShapeQuery） → 元素系统（附着/反应，读 ReactionTable）
  → 伤害计算（DamageFormulas，读策划案9.x公式） → EventBus 广播
  → UI / 任务系统 / 掉落系统 订阅响应
```

---

## 3. 角色与动作系统

### 3.1 角色节点结构

```
PlayerCharacter (CharacterBody3D)
├── CollisionShape3D
├── VisualRoot (Node3D)            # 模型根，朝向与受击闪白作用于此
│   ├── Skeleton3D + MeshInstance3D
│   └── AttachmentPoints (Node3D×N) # 武器/特效挂点
├── CameraRig (Node3D)
│   └── SpringArm3D（碰撞长度自适应，防穿墙）
│       └── Camera3D
├── AnimationPlayer / AnimationTree（StateMachine: idle/run/attack/skill/hit/climb/glide…）
├── HitBox (Area3D)                # 受击盒（常开）
├── AttackHitBoxes (Area3D×N)      # 攻击盒（按动画帧启停，监控 body_entered）
└── StateMachine (Node)            # GDScript 有限状态机，驱动 AnimationTree
```

要点：
- 移动用 `move_and_slide()`，重力自实现；跳跃土狼时间 0.1s、输入缓冲 0.15s（手感三件套）。
- 相机：SpringArm3D 长度受碰撞收缩；视角灵敏度、平滑插值参数集中于 `camera_config.tres`，便于调参。
- 完美闪避：受击前 0.2s 窗口由 StateMachine 记录 `dodge_start_time` 判定。

### 3.2 队伍与切换
- 场景内常驻 3 个 PlayerCharacter 实例，非激活者 `visible=false` + `process_mode=PROCESS_MODE_DISABLED`。
- **注意：`process_mode` 禁用不阻止 Area3D 信号派发与碰撞体存在**——非激活者必须同时将碰撞层置 0（`collision_layer = 0`，受击盒 `monitoring=false`），否则仍会被敌人索敌与命中；并从敌人仇恨列表移除。
- 切换 = 位置对齐 + 激活交接 + 1s 公共冷却（`PartyManager` 管理）；HP/MP/能量保存在各自 `CharacterRuntime`（JSON 侧数据），切换不重置。
- 切换取消后摇：旧角色技能动画结束帧前允许触发切换事件（StateMachine `can_cancel` 标志）。

### 3.3 攀爬与滑翔（第一期）
- 攀爬：向前射线检测表面法线，**倾角 ≥65° 的陡峭面**（缓坡直接行走）进入 Climb 状态，四向移动映射到墙面切线，体力耗尽进入 Fall。
- 蚀晶壁（策划案 8.2）：金元素技能命中墙面生成临时 `ClimbNode`（StaticBody3D + 标记），60s 超时消失。
- 滑翔：空中二段跳输入触发，垂直速度钳制在 -3 m/s，持续耗体力，取消条件（松开/落地/受击/体力尽）。

---

## 4. 战斗与元素系统（修正 C 类多项）

### 4.1 命中判定
- 近战：动画帧事件（`AnimationPlayer` 的 Call Method Track）启用/关闭 AttackHitBox，Area3D `body_entered` 收集目标，一帧内对每目标只结算一次（`attack_id` 去重）。**瞬时判定优先用 `PhysicsDirectSpaceState3D.intersect_shape()`（在判定帧同步查询，无信号时序问题）；Area3D 仅用于持续型判定（火域、光环）。**
- 投射物：`PoolManager` 池化 RigidBody3D（或 Area3D 直线模拟），命中/超时归还。
- 伤害数字：池化 `Label3D`，上浮+缩放动画，暴击变色放大。

### 4.2 元素附着数据结构（修正 B6/C：可实现版）

```gdscript
# 每个 CharacterBody3D（敌人）挂 ElementAura 组件
class ElementAura:
    var auras: Dictionary = {}   # element_name -> {gu: float, expire_tick: int}

    func apply(element: StringName, gu: float, now_tick: int) -> void:
        # 规则：同元素不叠加，取 max；衰减 -0.2GU/s
        # ICD：该敌人最近一次同类反应时间戳，2.5s 内不重复触发
    func try_reaction(incoming: StringName) -> ReactionData: ...
```

- 反应表为静态 Resource（`ReactionTable.tres`）：`key = "fire+metal"→ 熔金`，主导元素/倍率/效果字段与策划案 5.3 一一对应。
- 反应结算顺序：后手元素查表 → 检查 ICD → 消耗双方 GU → 派发效果（Buff 组件/DoT 组件/固定伤害）→ EventBus 广播 `reaction_triggered`（供成就/教学/UI 订阅）。

### 4.3 伤害计算
- `DamageFormulas` 静态类实现策划案 9.2 公式，输入纯数据输出纯结果（无副作用），**可在 tests/ 场景批量跑数值模拟**（配合 Excel 校表）。
- 等价交换：`EquivalenceComponent` 监听 R 键状态，技能释放前查询代价表（血/智/质），执行扣减（HP 保底 1 点规则在此组件内强制）后向 `DamageContext` 注入 `equivalence_mult`。

### 4.4 敌人 AI
- 第一期：GDScript 分层状态机（Patrol/Chase/Attack/Reposition/Stagger）+ `NavigationAgent3D` 寻路；不引行为树插件，避免过度工程。
- BOSS 三阶段：`phase_thresholds` 配置驱动，阶段切换广播事件（换攻击表/换场景机关：灼热地板、蒸汽安全区均为场景节点订阅事件响应）。
- 感知：距离+视野扇形判定（无需复杂感知系统）；同屏激活敌人上限 8（性能与难度双控）。

---

## 5. 世界与流式加载（修正 C2/C6/C7）

### 5.1 第一期：单场景 + 分组管理（刻意不做流式）
- 灰烬河谷 250×250m = 一个场景文件。**流式加载是第二期才面对的问题**，第一期引入只增加复杂度。
- 场景内以组（Group）与可见性管理：洞窟区域用 `VisibleOnScreenNotifier3D` + 距离开关（`process_mode` 禁用远处敌人 AI）。
- 地形：Blender 雕刻单块网格 + 手工摆放碰撞（`StaticBody3D` + 简化碰撞盒，不追求三角级精确）。

### 5.2 第二期：Terrain3D + Chunk 流式（先做 PoC 再全量）
- **性能 PoC 前置**：先做 2×2 chunk 原型验证加载 hitch 与内存，达标后再铺开。
- Terrain3D 插件生成自然地形（高度图 + 分区），自然地表之上的一切离散物体（树/石/建筑）按 **chunk（128m×128m）组织为 PackedScene**：
  - `WorldManager` 以玩家坐标计算所需 chunk 集合，差集加载/卸载。
  - 加载：`ResourceLoader.load_threaded_request()`；实例化分帧（每帧 ≤2 个）避免 hitch。
  - 卸载：`queue_free()` 常规物件，可复用重物（传送阵特效等）入 `PoolManager`。
- 植被：`MultiMeshInstance3D` 按密度图散布（脚本工具离线生成，不运行时计算）；按距离三档（近：全细节+碰撞 / 中：无碰撞 / 远：隐藏）。
- 导航：每 chunk 烘焙独立 `NavigationRegion3D`，加载时 `bake` 结果随场景载入（避免运行时烘焙卡顿）。

### 5.3 光照（修正 C7）
- 第一期：`DirectionalLight3D` + `WorldEnvironment`（ProceduralSky）+ 室内小范围 `VoxelGI`（仅洞窟）或干脆仅环境光提亮——**不做昼夜**（策划案已裁）。
- 第二期昼夜：固定若干关键帧光照参数插值；SDFGI 仅在 PC 性能预算内实测达标后启用，并作为画质选项而非默认。
- 阴影：单一 Directional 阴影（PSM），植被阴影距离限制 40m。

---

## 6. 炼成阵 Shader（修正 C8：给出可执行路径）

### 6.1 方案：贴地 Quad + 极坐标程序化 Shader
```
TransmutationCircle (MeshInstance3D: QuadMesh 贴地 0.02m 抬升)
└── shader 参数：
    inner_rings: int        # 内环数（基阵类型驱动：投射1/爆发3/召唤2）
    radial_segments: int    # 径向分割数（元素驱动）
    rune_atlas: texture2D   # 符文贴图集（8×8 符文，来源：公开域符文字体渲染成图集）
    element_color: vec3     # 元素主题色（火=橙红/水=青/金=黄铜）
    progress: float(0-1)    # 蓄力进度（驱动环旋转速度与发光强度）
    lifetime: float         # 存活时间（淡出）
```

### 6.2 Shader 核心逻辑（spatial, unshaded, additive blend）
- 极坐标 `atan(y,x)` 归一化后按 `radial_segments` 分割画刻度线（`step` + `smoothstep` 抗锯齿）。
- 同心圆环：距离场 `abs(length(uv) - ring_radius) < width`。
- 符文：每环按 `floor(angle * count)` 采样 rune_atlas 不同格子。
- 发光：`emission = element_color * (0.5 + progress)`，外圈亮度随 progress 提升；additive 混合实现多层阵叠加（多层 = 多个 Quad 不同半径）。
- 优势：**一个 Shader 表现所有基阵×元素组合**，参数化即变化——这正是旧方案"36 种组合做 36 套特效"问题的解。

### 6.3 技能特效分层
- 炼成阵（程序化 Shader）+ 主体爆发（GPUParticles3D，元素色统一由 `element_color` 驱动材质）+ 受击反馈（闪白 + 击退）。**特效按"元素色重着色"复用**，而非每技能独立制作。

---

## 7. 数据与存档（修正 C4）

| 数据 | 载体 | 说明 |
|------|------|------|
| 技能/物品/敌人/反应/掉落/任务定义 | **Resource（.tres，只读）** | 仅编辑器内制作；运行时 DataManager 深拷贝后使用；代码中禁止 `ResourceSaver` |
| 角色运行时（HP/MP/等级/经验/装备） | **内存对象 + JSON 存档** | 每 `CharacterRuntime` 映射存档节点 |
| 世界状态（传送点解锁/采集刷新/宝箱开启） | JSON 存档 | key = 节点路径/ID |
| 玩家设置 | `ConfigFile`（user://settings.cfg） | 与存档分离 |
| 本地化 | CSV key-value（`data/loc/zh.csv`） | 第一天就 key 化，禁止硬编码文案（远期加语言零重构） |

存档结构：
```json
{
  "schema_version": 1,
  "playtime_sec": 12345,
  "party": { "aiden": {"hp": 890, "mp": 120, "level": 12, ...}, ... },
  "inventory": [ {"id": "ore_iron", "count": 24}, ... ],
  "quests": { "main_01": "completed", "main_02": "active_2/3" },
  "world": { "teleports": ["tp_02"], "chests": ["chest_05"], ... }
}
```
- `SaveManager` 负责 schema_version 迁移（预留）；存档校验和防手改（单机轻度防呆即可）。

---

## 8. 动画资产规范（修正 C5/C6——最大工作量项，必须先定）

### 8.1 统一骨架与重定向
- 全部角色购买/制作时确认**人形骨架**（Mixamo 兼容）；项目内使用 Godot 4 骨骸重定向（Retarget）统一到项目基准骨架 `HumanoidBase`。
- 约束：腕/指骨骼可省（低模风格）；根运动（Root Motion）仅冲刺/击退用，其余原地动画+代码位移。

### 8.2 每角色动画清单（第一期 3 角色 × 约 19 条 ≈ 57 条）

| 类别 | 动画 | 来源 |
|------|------|------|
| 基础移动 (6) | idle / run / sprint / jump / fall / land | Mixamo |
| 探索 (3) | climb_idle / climb_move（横移与上下复用同一动画+代码倾斜，简化取舍）/ glide | Mixamo 改 |
| 战斗 (6) | attack_01-03 连段 / charged_cast / skill_cast / burst_cast | Mixamo 改 + 自调 |
| 反馈 (2) | hit / death | Mixamo |
| 通用 (2+) | 交互采集 / 切换入场姿势 | 自制 |

- 工作流：Mixamo 下载 FBX → Blender 清理（删根位移、统一朝向）→ glTF 导入 → Godot 重定向到 HumanoidBase → AnimationPlayer 归档。
- **每新增角色的动画成本 ≈ 3-5 个工作日**，立项新角色时按此核算。

---

## 9. UI 与输入

- 13 屏 UI（策划案第 10 章清单）：统一 `Theme` 资源（字号/色板/九宫格框），`Control` 布局 + Anchor 响应（最低 1280×720）。
- 输入：全部走 `InputMap` 动作名（`move_left`、`skill_e`…），代码零硬编码键位；手柄映射第二期。
- HUD 关键件：技能 CD 遮罩（`Range` + Shader 扇形）、Boss 霸体条、等价交换状态提示（R 键激活时屏幕边缘元素色渐晕）。

---

## 10. 性能预算与工程基础设施（修正 C7/C9）

### 10.1 性能预算表（第一期目标：GTX 1060 级 PC 稳 60 FPS）

| 预算项 | 目标 | 手段 |
|--------|------|------|
| 帧时间 | ≤16.6ms（CPU+GPU 各半） | Profiler 每周跑分记录 |
| DrawCall | ≤350 | 网格合并、MultiMesh、材质图集 |
| 同屏敌人 | ≤8 | 激活半径管理 |
| 粒子 | 同屏 ≤3 万面 | GPUParticles + 粒子 LOD（远距离简化） |
| 内存 | ≤2.5GB | 纹理压缩（S3TC/BPTC）、分辨率上限 |
| 加载 hitch | 单帧 ≤50ms（第一期场景常驻，理论无 hitch） | 第二期 chunk 化后每帧 ≤2 实例化 |

### 10.2 工程基础设施（个人开发者无 QA，工具即生命线）
- **Git + LFS**：`*.png/*.glb/*.wav/*.mp3` 走 LFS；每日提交，里程碑打 tag。
- **调试面板**（F3 开关，仅 debug build）：FPS/DrawCall/内存实时、玩家传送、无敌/秒杀开关、元素附着状态可视化、任务进度查看、时间缩放。
- **数值模拟测试场景**：`tests/` 下跑 DamageFormulas 与元素反应自动化用例（GUT 单测或手跑场景），改数值先跑测试再进游戏。
- **导出自动化**：写死导出预设 + 一键脚本（Godot CLI headless 导出 zip），每周产出可玩构建（dogfooding）。
- **占位资产规范**：所有未到位资产用统一灰色占位（材质名 `PLACEHOLDER`），发布检查脚本扫描禁漏。

---

## 11. 分期实现顺序与验收标准（修正 A2/A3）

### 第一期（对应策划案 P0-P4）

**P0 手感原型（全职 6-8 周）**
- 内容：角色控制器（移动/跳/冲刺）、SpringArm 相机、白模三段攻击与受击、1 个敌人追击 AI。
- 验收：自测连续游玩 30 分钟手感不烦躁；相机无穿墙抖动；攻击命中反馈明确（顿帧/闪白/击退）。**不达标不进入 P1。**

**P1 战斗核心（全职 10-12 周）**
- 内容：3 角色技能组、元素附着/ICD/5 反应、等价交换 3 代价、4 小怪 + 精英 AI、体力系统、伤害数字、HUD 战斗件。
- 验收：反应触发准确（自动化用例通过）；等价交换决策在实战中被自然使用（自测录像回看）；精英战 60-90s 且有博弈感。

**P2 区域内容（全职 12-16 周）**
- 内容：灰烬河谷地形/场景布设、攀爬滑翔、采集/宝箱/传送/3 谜题、BOSS 蚀核巨像三阶段。
- 验收：250m 区域探索 45-60 分钟不重样；BOSS 战完整使用 3 元素与 2+ 反应；60 FPS 达标（GTX 1060）。

**P3 系统整合（全职 8-10 周）**
- 内容：任务链 5 个、对话系统、背包/炼成/13 屏 UI、JSON 存档、设置。
- 验收：新玩家无引导文档可完整通关 Demo（找 3-5 人实测）；存档往返无损；全 UI 键盘可达。

**P4 打磨发布（全职 4-6 周）**
- 内容：音效音乐、性能冲刺、本地化 key 化收尾、itch.io/Steam Demo 页面。
- 验收：零 PLACEHOLDER 残留；崩溃率 <0.5%/小时；Demo 页面素材完成。

### 第二期新增（全职 12-18 个月，仅列技术要点）
- Terrain3D + chunk 流式（先 PoC）；2 新区域；6 元素与 10+ 反应（数据驱动扩展，不改代码）；完整养成（刻印词条系统——随机词条生成器 + 套装判定）；昼夜光照插值；手柄支持；Steam 成就/云存档集成。

---

## 12. 联机远期预留（修正 C3：只做抽象，不做实现）

- **现在做**：角色输入与状态经 `IActorSource` 接口读写（本地实现直接操作）；敌人状态变更经 EventBus 广播。此二者保证未来联机时"输入来源可替换为网络、状态可同步"。
- **现在不做**：任何 MultiplayerAPI 代码、主机迁移、房间系统。
- **远期启动时的评估项**（写入决策清单，启动前必答）：
  1. 中继方案：Steam Networking Sockets（需 Steam 上架）vs EOS vs 自建中继服务器（成本最高）；
  2. 同步模式：Host 权威（实现简单，房主体验差）vs 专用服务器（成本高）；
  3. 主机迁移：**默认不做**（房主掉线=房间解散，明示玩家）。

---

## 13. 外部依赖与风险清单

| 依赖 | 用途 | 风险与对策 |
|------|------|-----------|
| Terrain3D 插件 | 第二期地形 | 关注 Godot 版本兼容；PoC 前不深度绑定；备选：Blender 分块网格地形手工管理 |
| Mixamo 动画 | 全部角色动画 | 服务条款允许游戏使用；特殊技能动画需自制/外包补充 |
| Fab/Sketchfab 资产 | 模型 | **逐个核对商用授权**；统一风格指南先行 |
| Steam SDK | 第二期发行 | Godot 有 GodotSteam 封装，成熟可用 |

| 技术风险 | 等级 | 对策 |
|---------|------|------|
| 攀爬/相机手感打磨超期 | 高 | P0 全部时间给手感；参数全部配置化（运行时热调） |
| Jolt 物理 + CharacterBody 边界问题 | 中 | P0 即启用 Jolt 验证；遇阻回退默认物理（改配置即回退） |
| 元素反应数值崩坏 | 中 | 公式纯函数化 + 自动化模拟测试先行 |
| 单场景变大后编辑器卡顿 | 中 | 区域拆子场景嵌套；UI/世界/逻辑分文件 |
| 流式加载 hitch（第二期） | 中 | PoC 前置；分帧实例化；大对象池化 |

---

## 14. 总结

本技术方案对旧方案的三处关键纠偏：

1. **地形与渲染选型纠正**（GridMap/TileMap → Blender 网格 + Terrain3D；Forward+ 明确不支持移动端），消除了两个必返工的架构错误。
2. **把"动画资产 54 条、性能预算表、调试工具、Git LFS、数值自动化测试"这些旧方案完全缺失但决定生死的工作量显性化**，使排期从"29 周的幻觉"回到"全职 10-12 个月 Demo"的现实。
3. **联机从"阶段 6 就做"改为"抽象层预留 + 远期决策清单"**，避免在没有中继服务器方案的情况下投入注定返工的网络开发。

与 `策划案.md` 四期路线一一对应，可直接按 P0 里程碑开工。
