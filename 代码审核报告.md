# 代码审核报告

- 审核人：主程序 / 代码审核经理
- 报告范围：按开发阶段滚动更新（P0 → P1 → P2）
- 审核方式：逐文件静态审查（对照 `策划案.md` V2.1 / `技术方案.md` V2.1）+ 动态测试（每个修复先写可复现用例）+ 主场景冒烟扫描

> **2026-09-11 勘误**（汇总与理由见 `进度报告.md` §11）：
> ① 原章节编号"第一部分（P2）/第三部分（P1）/第四部分（P0）"存在跳号（"第二部分"从未存在），已重排为 第一（P2）/第二（P1）/第三（P0）（E4）；
> ② 第二部分轮次表"第一轮 8 个问题"与"第二轮 3 个真 bug"合计 11，与问题名单 8 条矛盾，按"两轮合计 8"修正第一轮为 5 个（E3）；
> ③ 第二部分表中"新增 26 项 P1 用例"按阶段总量差修正为 50（E2）。

---

# 第一部分 · P2 区域内容·第一轮（2026-09-10）

## 一、交付范围（技术方案 §11 P2 的本轮切片）

| 模块 | 内容 | 对应文档 |
|------|------|---------|
| 攀爬 | ≥65° 陡面自动扒墙（法线判定 dot(n,UP)≤cos65°）、墙面四向 2.5m/s、体力 10/s、跳离/翻越墙沿/耗尽坠落 | 策划案 §8.2 / §5.2 / 技术 §3.3 |
| 滑翔 | 空中再按跳跃展开、垂直钳制 -3m/s、体力 8/s、再按/落地/受击/体力尽取消 | 策划案 §8.2 / §5.2 |
| BOSS·蚀核巨像 | level 20（≈10060 HP，对齐 §9.3"BOSS 3-5 分钟"口径）；一阶段砸击+冲撞（霸体 200）；二阶段（70%）周期金属护壳（自挂金附着 4GU、金抗+80%、承伤-70%，**火打熔金直接削壳**+3s 硬直窗口）；三阶段（30%）灼热地板（5/s 灼烧）+攻速+30%+承伤+50%，水技能打地板生成蒸汽安全区（8s 免灼烧） | 策划案 §5.4 / §9.2 |
| 交互系统 | Interactable 基类 + F 键 2.5m 最近对象扫描 + HUD 提示；采集点（铁矿/药草/水镜露入队包）、宝箱（不刷新）、传送阵（解锁+环序传送） | 策划案 §8.1 / §10 |
| 元素谜题原型 | 炼成灯（火附着点亮，与怪物同一套附着管线）×3 → 密门开启；完整 3 谜题在 P2 后续轮次 | 策划案 §8.1 |
| 灰烬河谷白模 V2 | 250×250 三段结构（矿镇→河谷→洞窟）、攀爬教学墙+墙顶宝箱、布设接入 main（旧竞技场保留作试练场） | 策划案 §8.1 |

## 二、本轮发现并修复的问题（两轮闭环）

### 第一轮（开发中即时修复，全部由新增用例或编译门禁抓取）
1. **【严重】三角色配置错位复发变体**：`puzzle_door.tscn` 使用非法构造语法（`BoxMesh(SubResource(...).size)`）导致场景解析失败——重写为标准 BoxMesh 子资源。（另：P1 的 `member.stats` 未覆盖问题在 P2 代码复审中被再次确认已修复。）
2. **【中】`puzzle_lamp.gd` 逻辑死结**：`get_health()` 返回 null 会被 `Combat.resolve_attack` 提前拦截，火附着永远到不了灯上、谜题无法点亮。修复：灯改为"超大血量（10⁹）可命中对象"，与怪物共用附着管线，监听自身火附着事件点亮。
3. **【中】`SteamZone` 从未加入 "steam_zones" 组**——`GROUP` 常量定义后没有 `add_to_group`，`covers()` 永远 false，蒸汽安全区完全无效。修复：`_ready` 入组。
4. **【中】BOSS 二→三阶段推进被破壳硬直阻塞**：破壳硬直 3s 期间不跑 `_tick_chase`，阶段判定停摆——属设计内行为（输出窗口），测试改为直接调用 `_update_phase()` 验证机制而非等待时序。
5. **【低】BossColossus 局部变量 `player` 重复声明**（编译失败）；`ash_valley.gd` 宝箱 preload 路径误写 `scenes/common/`（实际 `scenes/world/`）。均为编译门禁抓取，即改。
6. **【低】测试脚本 `fire_ctx` 同作用域重复声明、`b_pos` 类型推断失败**——重命名/显式类型。

### 第二轮（交付前全量门禁）
- 干净重导入 + 单元 + 集成 + 冒烟扫描：**单元 131/131、集成 60/60、严重错误 0**。

## 三、设计决策与文档偏差（主策划口径）

1. **护壳承伤 -70%、灼烧 5/s、蒸汽区 8s/半径 3m、BOSS 砸地 60/冲撞 51**——策划案 §5.4/§9.2 未给数值，均为首版可调项（集中在 `boss_colossus.gd` 常量与 `heat_zone.gd`）。
2. **护壳实现**=BOSS 自挂金附着 4GU 并持续维持；玩家火后手触发熔金 → `Combat` 检测 `molten_gold` 命中带 `break_shell` 的目标 → 削壳+3s 硬直。与文档"熔金反应可直接削除护壳"逐字对应。
3. **采集刷新**：按文档"重新读档后刷新"，存档映射 P3 接入，白模当前一次性。
4. **P2 验收"45-60 分钟不重样"与"60 FPS"**：内容密度本轮为白模骨架，待后续轮次填充后实测；60FPS 需实机验证。

## 四、P2 验收对照（部分，本轮切片）

| 验收项 | 状态 |
|--------|------|
| 攀爬滑翔 | ✅ 集成：扒墙上移、滑翔 -3m/s 钳制、体力消耗 |
| BOSS 三阶段完整使用 3 元素与 2+ 反应 | ✅ 机制全齐（护壳熔金/破甲/灼烧/蒸汽），体感待实机 |
| 采集/宝箱/传送/谜题 | ✅ 采集/宝箱/传送/谜题原型自动化验证通过 |
| 250m 区域 45-60 分钟不重样 | ⏳ 白模骨架就绪，内容填充为 P2 后续轮次 |
| 60 FPS（GTX 1060） | ⏳ 需实机测量 |

## 五、P2 交付后深度复审（2026-09-11，第三轮：按"检查→修复→再查"流程执行）

复审对象为 P2 全部新增代码，重点排查测试覆盖不到的设计级问题（跨对象生命周期、场景布设一致性、表现层误用）。**发现 5 个问题，全部修复**：

| # | 严重度 | 问题 | 根因 | 修复 |
|---|--------|------|------|------|
| R-1 | **高** | BOSS 死亡后灼热地板永久残留——三阶段生成的 HeatZone 挂在场景而非 BOSS 身上，BOSS 死亡无人清理，洞窟将永久灼烧无法再战 | 跨对象生命周期未闭环 | `BossColossus._on_died` 重写：先清除 `_heat` 再调父类 |
| R-2 | 中 | 边界墙仅 6m 高，玩家可攀爬翻越出图坠入虚空 | 边界墙与攀爬系统的交互未评估 | 边界墙加高至 20m（攀爬体力上限 100 需 200，不可及） |
| R-3 | 中 | 玩家出生点与灰烬河谷布设错位：`main.gd` 沿用 P0 竞技场坐标 (0,0,10)，应出生于矿镇 (0,0,96) | 场景迭代后常量未同步 | 出生点对齐矿镇并注释与 ash_valley 的耦合 |
| R-4 | 低 | BOSS 出生点 (0,0,-110) 与洞窟后墙（占位 -110~-109）相交，出生即陷入墙体 | 布设坐标核对缺失 | BOSS 移至 z=-104 |
| R-5 | 低 | 开宝箱误用伤害数字通道发送 0 值，画面弹出"0"字样 | 表现层通道误用 | 改用炼成阵光环（TransmuteRing）作开启反馈 |

**复审确认无问题的项**：攀爬/滑翔与角色切换、受击的状态机重入边界；TeleportGate 静态解锁表跨重载一致性；PuzzleLamp 事件解联防泄漏；BOSS 冲撞/砸击与硬直、破霸的叠加行为；交互扫描的组过滤与距离阈值。

**修复后再次自查（第二轮门禁）**：干净重导入 + 全量回归 + 冒烟扫描——**单元 131/131、集成 60/60、严重错误 0**。

## 六、外部深度审查（2026-09-11，第四轮：全代码 + 设定一致性核对）

审查范围：全部脚本（约 4400 行）逐文件 + `策划案.md` V2.1 / `技术方案.md` V2.1 数值与规则逐项对照（反应表 5 条数值、公式锚点、技能倍率/GU、敌人 DEF/抗性/HP 公式、体力/MP/能量规则、等价交换三祭献均核对一致）。**定级：严重 3 / 警告 5（全部修复）/ 建议 4（挂账）**：

| # | 级别 | 问题 | 根因 | 修复 |
|---|------|------|------|------|
| X-1 | **严重** | 敌人索敌 / 灼热地板 / 调试面板把隐藏队友当目标：`"player"` 组含全部 3 名常驻角色，`_find_player` 取组内首个——切人后敌人追旧角色的冻结站位；BOSS 三阶段 HeatZone 遍历整组，会把留在场内的后台角色持续灼烧致死 | 技术方案 §3.2"非激活者须从敌人仇恨移除"未落地 | `PartyManager` 维护 `player_active` 专用组（切换时增删）；`EnemyBase._find_player` / `HeatZone` / `DebugPanel` 改用该组，空组回退 `"player"`（兼容独立模式测试） |
| X-2 | **严重** | BOSS 护壳任意反应皆可削除：火直打金壳触发的是**淬火脆化**（金先火后），旧实现 `reaction != null` 即调 `break_shell`——"必须火先金后打熔金"的双角色配合被架空，与策划案 §5.4 / §9.2"熔金反应可直接削除护壳"直接矛盾（原集成测试也锁定了错误行为） | Combat 削壳条件无判定；BOSS 自挂护壳经 `apply_incoming` 还会在带火/水附着时自触反应 | `ReactionDefinition` 新增数据字段 `breaks_shell`（仅熔金置 true，数据驱动）；Combat 按该字段门控；`ElementAura` 新增 `set_aura()/clear()`（护壳直挂绕过反应管线，替代原 `aura_elements().erase()` 内部状态黑魔法）；集成测试改为双向断言（火直打=淬火不削壳；火先金后=熔金削壳） |
| X-3 | **严重** | 玩家死亡信号链断裂：`PlayerCharacter._on_died()` 从未被连接——`EventBus.player_died` 事件、独立模式 1.5s 复活计时器、`_dead` 标志（死亡后仍可施法的守卫）全部为死代码 | 只连接了 `damaged/healed`，漏接 `died` | `_bind_runtime` 补连接（带重连守卫） |
| X-4 | 警告 | 水弹 `collision_mask=2` 只含玩家层——撞不到世界几何，穿墙飞行只能等 3s 超时 | 掩码漏配世界层 | `water_bolt.tscn` mask 2→3（world+player） |
| X-5 | 警告 | HUD 硬编码中文（"就绪/充能/★R 已激活"）违反本地化纪律（技术方案 §7：第一天就 key 化） | 战斗件补写时绕过 zh.csv | 新增 `HUD_SKILL_READY / HUD_SKILL_CHARGING / HUD_EQUIV_ACTIVE` 词条并改走 `tr()` |
| X-6 | 警告 | 卡文「涌泉之环」治疗基数用 `cfg.base_hp`（1 级基础值）——等级>1 后治疗不随成长，与设计决策"12%×卡文最大HP"不符 | 基数取错 | 改 `p.runtime.max_hp`（1 级数值不变，无测试影响） |
| X-7 | 警告 | 调试面板 T 传送硬编码 P0 竞技场坐标 (0,2,10)——灰烬河谷会传到河谷中部而非出生点；无敌/传送还可能作用到隐藏队友 | 场景迭代后调试常量未同步 | 传送改 `player.spawn_point()+UP`（新增访问器）；操作对象改激活组 |
| X-8 | 警告 | `DataManager.config` 仅用 `assert`——release 构建剥离 assert 后配置缺失会以 null 静默穿透到调用方崩溃且无日志 | assert 语义误用 | 补 `push_error` + 显式返回 null（debug 行为不变） |

**修复后门禁（第四轮自查）**：干净重导入 + 全量回归 + 冒烟——**单元 131/131、集成 61/61（削壳断言扩为双向，60→61，合计 192）、主场景冒烟 300 帧 0 `SCRIPT ERROR`**。

**建议级（挂账，未改代码，待策划/所有者裁定）**：
1. **Q 命中返还 +8 能量**：策划案 §9.1 字面为"炼成技命中+8"（E 专属），现实现 E/Q 均返（集成测试注释表明有意为之）——需口径裁定。
2. **ICD 实现口径**：策划案 §5.3-3 写"附着施加与反应触发共用 2.5s 间隔"，实现为"仅反应触发设置 ICD"（`element_aura.gd` 头注已声明取舍，玩家"普攻挂元素→换元素触发"循环更顺）——建议策划案补注实现口径。
3. **BOSS 二阶段削壳死路**：若玩家未预挂火附着即进入护壳期，火被淬火拦截、金同元素无反应，护壳无法经反应削除，只能以 ×0.3 承伤硬磨至三阶段——建议护壳重挂留 1-2s 附着窗口，或 70% 转阶段时清一次既有附着。
4. **每帧扫描**：`find_interactable` 每物理帧遍历组、敌人调试标签每帧字符串拼接——当前 26 个交互物规模无感，P3 UI 扩量前再优化。

## 七、P2 第二轮交付审查（2026-09-11，第五轮：随开发"检查→修复→再查"两轮闭环）

审查对象为第二轮全部新增代码（5 个新脚本 + 3 个新场景 + 8 处既有文件修改 + 14 项新测试）。**发现并修复 6 项（含 2 项严重）、记录 2 项建议**：

| # | 级别 | 问题 | 修复 |
|---|------|------|------|
| Y-1 | **严重** | 独立模式 `PlayerCharacter`（无队伍）从不推进 `runtime.tick_resources`——E 冷却与 MP 永久冻结（集成测试抓取：共享输入施放 E 后冷却卡死、后续施法全被拦） | `_physics_process` 中 `party == null` 时自行 tick |
| Y-2 | **严重** | .tscn 文本格式的 `Color()` 构造必须 4 参数（含 alpha），3 个新场景全部解析失败（导入门禁抓取，单元可加载性 3 项转红） | 全部补第 4 参数 |
| Y-3 | 警告 | 抓握点首格生成在射线命中点上方 1.2m，高于站立玩家的墙面探测高度（胸口 +1.2m），无法进入攀爬 | 首格贴近命中点（+0.2m），其后每 1.6m 一格 |
| Y-4 | 警告 | `GameSaveFlow.save/load` 与全局内置 `load()` 同名——类内裸调用会误解析为静态方法 | 改名 `save_game/load_game`（与 SaveManager 命名对齐） |
| Y-5 | 警告 | 洞窟一次性采集登记不校验 `node_id` 非空——空 ID 节点会污染存档的 cave_gathered 列表 | 补 `node_id != ""` 守卫 |
| Y-6 | 警告 | 连按 E 抓握点无限堆叠（重复施法无去重，物理与视觉双重污染） | 基点 2m 内已有抓握点则跳过本次生成 |

**建议级（记录，未改）**：① 死亡状态存档读档后以 1 HP 复活（`apply_save` 的 clamp 下限，白模可接受，P3 死亡/重生屏一并处理）；② F5/F9 为策划案 §5.1 键位表之外的临时键位（P3 存档 UI 的过渡形态）。

**门禁（两轮自查后）**：干净重导入 + 全量回归 + 冒烟——**单元 134/134、集成 72/72（合计 206）、主场景冒烟 300 帧 0 `SCRIPT ERROR`**。

---

# 第二部分 · P1 战斗核心（2026-09-10，历史存档）

## 一、结论

P1 范围（技术方案 §11：3 角色技能组 / 等价交换 3 代价 / 4 小怪 + 精英 AI / MP·能量 / HUD 战斗件）已实现。开发周期内按"检查→修复→再检查"跑了两轮完整闭环：

| 轮次 | 手段 | 发现 | 修复后状态 |
|------|------|------|-----------|
| 第一轮（开发中即时） | 新增 ~~26~~ **50** 项 P1 用例随写随测（勘误口径：阶段总量差） | ~~8~~ **5** 个问题 | 单元 118/118、集成 40/43 |
| 第二轮（交付前全量） | 干净重导入 + 全量回归 + 主场景冒烟错误扫描 | **3 个真 bug**（含 1 个最重问题） | 单元 118/118、集成 **43/43**、冒烟 **0 脚本错误** |

## 二、本轮发现并修复的问题

### 【严重】#P1-1 三名角色全部是艾登——`PartyManager.setup` 未覆盖 `member.stats`
- **现象（测试抓取）**：诊断输出 `member0/1/2 = CHAR_AIDEN`，莉赛尔的"智之炼成"实际执行的是血祭，卡文的"质之炼成"同样错位。
- **根因**：玩家场景 `player_character.tscn` 的 `stats` 硬编码为 `char_aiden.tres`；`PartyManager.setup` 接收了三份配置却只用于染色，**从未赋给 `member.stats`**，而运行时真正读取的是 `member.stats`。
- **修复**：`setup` 中显式 `member.stats = cfg`（在 `add_child` 前注入），并加注释说明场景默认值会覆盖的陷阱。
- **验证**：集成测试断言三成员元素分别为金/火/水；智祭/质祭数值用例转绿。

### 【严重】#P1-2 `CharacterRuntime` 未声明 `hp` 字段
- **现象**：切换角色时 `Invalid assignment ... 'hp' ... CharacterRuntime`，队伍切换链路整体失效。
- **根因**：技术方案 §3.2 要求"HP 保存在 CharacterRuntime"，实现类只声明了 `max_hp`。
- **修复**：补 `hp` 字段并在 `setup` 初始化；组件信号回写 runtime（视图/存储分离）。
- **验证**：切换往返后"半血仍是半血"用例通过。

### 【严重】#P1-3 `main.gd` 先 `setup` 后 `add_child`——装配发生在树外
- **现象（第二轮冒烟扫描抓取）**：主场景启动 4 个脚本错误（`get_children` on null ×3、`camera` on Nil ×2），成员的 `_ready` 未执行、`@onready` 全为 null，游戏靠树进入后的延迟初始化"侥幸"跑起来。
- **根因**：`party.setup(...)` 在 `add_child(party)` 之前调用，成员被挂进一棵尚不在树内的子树。集成测试恰好用了正确顺序，未能复现——这正是"集成绿 ≠ 主场景对"的典型案例，冒烟扫描因此列为常驻门禁。
- **修复**：`add_child(party)` 提前到 `setup` 之前。
- **验证**：冒烟脚本错误 4 → **0**。

### 【中】#P1-4 `_set_active` 初始装配不禁用其他成员（审核 R9 复发）
- **现象**：开场时莉赛尔/卡文带碰撞与可见状态驻场。
- **修复**：`_set_active` 统一为"激活目标、禁用其余"（无论 initial 还是切换）。
- **验证**：集成用例"非激活角色关碰撞且隐藏"。

### 【中】#P1-5 削霸链路整体失效——`StatusHolder.poise_damage_mult()` 缺失
- **现象**：`PoiseComponent` 调用不存在的方法，削霸伤害计算每次报错归零。
- **修复**：补方法（复用淬火的 `stagger_mult` 数值，策划案 §9.4 同源）。
- **验证**：Poise 单测 7 项 + 精英破霸集成用例。

### 【中】#P1-6 `Skills` 脚本 Variant 推断编译失败
- `var freeze := cfg.skill_q_freeze * eq.get(...)`——`eq.get` 返回 Variant 无法推断类型。修复为显式 `float` 类型声明（`eq.get` 结果强转）。此类错误编译期暴露，import 门禁扫描已覆盖。

### 【低】#P1-7/#P1-8 测试自身缺陷两处（非产品代码）
- MP 恢复用例未考虑 82 点上限钳制导致断言溢出；能量断言未隔离"切换入场 +10"的累计。已修正期望值/前置清零。
- 方法论修正：技能烟测最初在**物理帧之外**直接调用 `Skills.cast`，`intersect_shape` 在该上下文不可靠导致永远落空——已改为经 `Input.action_press` 走真实物理帧输入驱动（与正式游玩路径一致）。

## 三、P1 设计决策与文档偏差（主策划口径）

1. **精英取 level 18**：策划案 §9.3 的"精英战 60-90s"校验是 25 级玩家口径；P1 白模玩家固定 1 级（DPS ≈ 30），精英 HP ≈ 2746 / DEF 90 时实测落在验收区间。正式 Demo 玩家练度上来后配置回正。
2. **质之炼成"效果持续 ×2"从宽解释**：对持续型效果（火域、锈蚀 DoT）统一 ×2 时长；文档原文只提"附加反应效果"。
3. **卡文治疗量** = 12% × 卡文最大 HP × (1 + EM/400)（文档未给公式，EM 联动按剧变公式同构）。
4. **淬火脆化第二效果**（霸体恢复暂停 3s）随反应效果时长乘区（质祭下 6s）。
5. **傀儡砸地 1.3× / 猎犬突进 0.8× 基础伤害**、削霸数值（普攻 5/5/8、E 20、Q 40）、E 冷却 6-8s 均为首版可调常量/配置，文档未定。
6. 竞技场沙盒倍率（矿工 8× 血）与补刷（6s）仅白模，正式规则 P2。

## 四、P1 验收对照（技术方案 §11）

| 验收项 | 状态 |
|--------|------|
| 3 角色技能组（E/Q × 3 形态各异，倍率按 §9.2） | ✅ 集成：E 直线命中、Q 能量消耗、霸体 5s |
| 元素附着/ICD/5 反应 | ✅ 单元 26 项 + 集成管线精确数值（125.0/148.0） |
| 等价交换 3 代价（血/智/质，含 1HP 保底、素材不足拒绝） | ✅ 集成数值断言全过 |
| 4 小怪 + 精英 AI | ✅ 矿工/猎犬/傀儡/水元素 + 精英（霸体、破霸易伤、双阶段、召唤） |
| 体力系统 / 伤害数字 / HUD 战斗件 | ✅ P0 已有 + P1 新增 MP/能量/技能 CD/队伍栏/R 渐晕/炼金尘 |
| 反应触发准确（自动化用例） | ✅ |
| 精英战 60-90s 博弈感 | ⏳ 数值配置落位（level 18），实机体感待所有者玩测 |
| 等价交换在实战中被自然使用 | ⏳ 机制就绪，待玩测观察 |

---

# 第三部分 · P0 手感原型（2026-09-10，历史存档）

---

## 一、问题清单（全部已修复）

### #1 玩家受击击退被归零 【严重 · 战斗手感】
- **现象**：玩家被敌人命中后只有向上的小弹跳，没有水平击退。
- **根因**：`player_character.apply_hit_reaction()` 先把击退写入 `horizontal_velocity`，随后 `fsm.transition(&"hit", {})` 传了**空字典**；`HitState.enter()` 从 msg 取 `knockback`（默认零向量）再赋值回 `horizontal_velocity`——刚设置的击退被覆盖。
- **修复**：击退量经 msg 显式传递（`{"knockback": horizontal_velocity}`）；`HitState.enter` 加注释声明 msg 是唯一来源、默认值仅兜底。
- **验证**：集成测试"敌人攻击命中玩家"通过；击退方向与大小随 `combat_config.knockback_*` 生效。

### #2 `enemy_died` 信号只声明、从未发射 【严重 · 系统契约】
- **现象**：`EventBus.enemy_died` 全项目零个发射点。任务/UI/成就系统永远收不到击杀通知——这在 P1 接任务链时会直接变成"杀怪不计数"。
- **根因**：设计时把发射点放在 `Combat.resolve_attack`，落地时遗漏；且放 Combat 本身也不对——DoT（锈蚀）击杀不走 `resolve_attack`，会漏发。
- **修复**：发射点移至 `EnemyBase._on_died`（单一来源：直接伤害、DoT、未来的环境击杀统一覆盖）。
- **验证**：集成测试两条击杀路径均断言"恰好一次、两敌人事件互不串扰"（按实例 id 收集）。

### #3 连段缓冲窗口过窄，前摇段按键被吞 【中 · 战斗手感】
- **现象**：只有攻击判定帧（active）之后的按键才能排队下一段；在起手/前摇段按键直接丢失——玩家实际体验为"连招总是断"。
- **根因**：`AttackState` 排队条件写成 `_resolved and wants_attack()`。
- **修复**：进入攻击态后任意时刻按键都排队；用 `_entry_physics_frame` 排除进入帧本身，否则起手那一次按键会被当成连段键导致无需输入的自动三连。
- **验证**：集成测试三段连击命中 3 次、第三段金附着 GU=1.00；真机手感待 P0 玩测复核。

### #4 对象池悬挂引用会在场景切换后崩溃 【中 · 健壮性】
- **现象**：`PoolManager` 是 Autoload，寿命长于场景。场景整体释放后，池内登记的是已释放节点；下次 `spawn` 返回悬空实例 → 运行时崩溃/未定义行为。P0 主循环不切场景所以没炸，属**埋雷**。
- **修复**：`spawn` 取用前用 `is_instance_valid` 逐个校验并剔除失效节点。实现细节：池内取出必须走**无类型 Variant 中转**——已释放实例赋给 `Node` 类型变量、甚至对它用 `is` 运算，都会直接报脚本错误。
- **验证**：单元测试"复用同一实例 / free 后池自愈新建"通过。

### #5 场景计时器悬挂回调 【中 · 健壮性】
- **现象**：玩家重生（1.5s）与假人复活（3s）用 `get_tree().create_timer(...).timeout.connect(...)`；若计时期间场景被释放，回调打在已释放对象上报错。同属埋雷，P3 加场景切换后必炸。
- **修复**：改用**子节点 `Timer`**（随宿主自动释放，且暂停语义正确——暂停时倒计时一并冻结）。
- **验证**：现有死亡/复活流程回归通过；场景树检查无游离计时器。

### #6 hitstop 覆盖调试时间缩放 【低 · 工具链】
- **现象**：顿帧结束硬编码 `Engine.time_scale = 1.0`；调试面板设置的 0.5x / 2.0x 会被任何一次命中打回 1.0。
- **修复**：进入顿帧前记录当前缩放，结束时恢复"进入前"的值。
- **验证**：集成测试：外部设 2.0 → hitstop 压到 0.1 → 结束恢复 2.0。

### #7 存档无校验和（文档要求未落地） 【中 · 合规】
- **现象**：技术方案 §7 明确"存档校验和防手改（单机轻度防呆即可）"，首版没做。
- **修复**：存档文件格式改为"首行校验和（正文文本 hash）+ JSON 正文"；校验失败/缺校验和一律拒绝加载。选文本 hash 而非 Dictionary.hash，是因为 JSON 往返会把 int 变 float，结构级 hash 不可靠。
- **验证**：单元测试：正常往返无损 / 篡改正文被拦截 / 裸 JSON 被拒绝 / 删除后不再视为有存档。

### #8 本地化回退缺失（文档要求未落地） 【中 · 合规】
- **现象**：`zh.csv` 只有 `zh_CN` 列，工程默认语言非中文的系统上 `tr("HUD_HP")` 会返回 key 本身，HUD 显示 "HUD_HP"。
- **修复**：`project.godot` 增加 `internationalization/locale/fallback="zh_CN"`（符合"本地化骨架，远期加语言零重构"的文档要求）。
- **验证**：单元测试断言 `tr("HUD_HP") == "生命"`、缺失 key 回退不崩溃。

### #9 敌人间互相穿插叠成一团 【低 · 表现】
- **现象**：敌人碰撞掩码只有世界+玩家（mask=3），多敌追击时彼此穿透、重叠成一个胶囊。
- **修复**：`enemy_miner.tscn` 碰撞掩码 3 → 7（加入 enemy_body 层，敌人互相阻挡）。
- **验证**：集成测试敌人追击/攻击行为无回归。

### #10 局部变量遮蔽 `Node3D.show()` 【低 · 规范】
- **现象**：`enemy_base._update_debug_label` 里 `var show` 遮蔽同名方法，产生编译警告级噪音。
- **修复**：改名 `visible_now`。

---

## 二、测试账本（审核后全绿）

| 套件 | 用例数 | 覆盖 |
|------|--------|------|
| `tests/test_runner.tscn`（单元/规则） | **87 项全过** | 策划案 9.2/9.3 公式锚点（13/81/155/240/646/2288 等）、5 反应表数据与方向性、附着 max/衰减/消耗残留/ICD 2.5s、状态时效（减速/破甲/DoT/硬直强化）、生命与体力边界、全部 .tres/.tscn 可加载性、本地化回退、池复用与自愈、存档往返+防篡改 |
| `tests/integration_runner.tscn`（端到端） | **20 项全过** | 模拟真实输入：三段连击（命中 3 次、伤害 45.1 ≈ 基准 44.5、金附着 1GU）、白雾增幅精确 125 / 熔金 148（含剧变加算）流经 Combat 全管线、ICD 拦截、移动位移、敌人 7m→1.3m 追击并命中玩家、enemy_died 两路径恰好一次、hitstop 恢复语义 |
| 主场景冒烟 | 240 帧 | 零脚本错误 |

运行方式（CI 可直接挂）：
```bat
Godot_console.exe --headless --path F:\tools\GL res://tests/test_runner.tscn
Godot_console.exe --headless --path F:\tools\GL res://tests/integration_runner.tscn
```

## 三、挂账事项（不构成 P0 阻塞，P1 处理）

1. 敌人重力/转向速率硬编码（22/40/10.0）——P1 建 `EnemyMovementConfig` 收编。
2. 玩家无受击无敌帧，多敌人围殴可被无限连——与 P1"完美闪避（受击前 0.2s 子弹时间）"一起设计。
3. 敌人会持续攻击死亡状态的玩家（无伤害，`resolve_attack` 早退兜底）——P1 仇恨列表时一并处理死亡脱战。
4. `melee_query` 每次判定 new 一个 `SphereShape3D`——帧内小对象，量小无感；P1 技能系统接入时统一做形状缓存。
5. `heavy_attack`（长按蓄力）动作已注册未实现——P1 范围（策划案 5.1/5.2）。
6. 敌人 AI 无巡逻行为（原地待机→仇恨），策划案 P0 只要求追击链路，巡逻 P1 补。
7. 测试进程退出时的 RID/ObjectDB 泄漏提示来自 `--quit-after` 强退与测试故意留下的池探针节点，均为已知噪音，非产品代码问题。

## 四、审核方法说明

- 静态：对照技术方案 §2-§12 逐节核对节点结构/数据流/数据分工（Resource 只读 vs JSON 运行时）；对照策划案 §5/§6/§9 核对规则与数值来源；专项排查事件信号"声明-发射-订阅"三方闭环（发现 #2 即此法）。
- 动态：每个修复项先写**能复现问题**的测试再修复（如 #4 的池自愈、#7 的篡改用例），修复后全量回归。
- 手感类验收（连玩 30 分钟不烦躁、相机实际观感）无法自动化，维持原结论：**代码侧就绪，待所有者实机玩测放行 P1**。
