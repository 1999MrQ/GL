# 进度报告 · 供协作者共同开发

- 更新日期：2026-09-11
- 维护人：主程序 / 主策划 / 代码审核经理（随每轮开发滚动更新）
- 仓库：https://github.com/1999MrQ/GL（分支 main）
- 引擎：**Godot 4.4.1-stable（全组锁定此版本）** / GDScript / Forward+ / Jolt 物理
- 文档基线：`策划案.md` V2.1 + `技术方案.md` V2.1 为唯一开发依据；过程记录见 `开发记录.md`；历次问题清单见 `代码审核报告.md`

---

## 1. 项目速览

《等价之理：哲学家的旷野》——以"等价交换"为战斗与世界观核心的 3D 开放世界动作 RPG（个人开发体量，PC 单机买断）。四期路线：**垂直切片 Demo → 正式版 1.0 → 内容扩展 → 远期联机**。

当前状态：**P2 区域内容·第二轮已交付**（谜题 3/3、观景点 ×2、蚀晶壁齐备；采集/宝箱/传送/队伍已接入存档 F5/F9）。工程为白模（占位灰模 + 程序化表现），玩法系统完整可玩，正式美术/音频资产未接入（P2 第三轮与 P4）。

## 2. 阶段进度总览

| 阶段 | 内容 | 状态 | 备注 |
|------|------|------|------|
| P0 手感原型 | 角色控制器 / 相机 / 三段连击与命中反馈 / 敌人追击 AI | ✅ 完成 | 实机玩测通过（2026-09-10） |
| P1 战斗核心 | 3 角色切换 / 六技能 / 等价交换 / 4 小怪 + 精英 AI / MP·能量 / HUD 战斗件 | ✅ 完成 | 自动化验证通过；"精英战 60-90s 博弈感"待实机体感确认 |
| P2 区域内容 | 攀爬滑翔 / 交互系统 / BOSS 蚀核巨像三阶段 / 灰烬河谷白模 / 谜题×3 / 蚀晶壁 / 观景点 / 存档流 | 🔨 **第二轮已交付** | 剩余：地形网格替换灰盒、45-60 分钟内容密度实测、60FPS 实测 |
| P3 系统整合 | 任务链 / 对话 / 背包 / 13 屏 UI / 完整存档 | ⬜ 未开始 | 存档已接世界/队伍数据（F5/F9 白模临时键位，GameSaveFlow 编排）；13 屏 UI 与任务链未开始 |
| P4 打磨发布 | 音效音乐 / 性能冲刺 / 本地化 / Demo 页面 | ⬜ 未开始 | |

## 3. 已交付内容明细

### P0 手感原型（commit `34b7c60` 合并交付）
- 角色控制器：`CharacterBody3D` + 自实现重力 + **土狼时间 0.1s / 跳跃缓冲 0.15s / 相机相对移动**；冲刺耗体力 15/s、停 2s 后 20/s 回复
- SpringArm 越肩相机（防穿墙），参数全配置化（`camera_config.tres`）
- 白模三段连击（帧数据驱动 + 连段缓冲），第三段附 1GU 角色元素；命中反馈：**顿帧 / 闪白 / 击退 / 池化伤害数字**
- 数值核心：`DamageFormulas` 纯函数（策划案 §9.2/9.3 逐锚点校验）；`ElementAura`（GU 取 max / -0.2GU·s 衰减 / 单一 ICD 2.5s）；`ReactionTable.tres` 数据驱动 5 反应
- 敌人追击 AI、训练假人、F3 调试面板、InputMap 全动作化、本地化 CSV、JSON 存档骨架（schema v1 + 校验和）

### P1 战斗核心（commit `34b7c60`）
- **三角色常驻与切换**：艾登（金·近战）/ 莉赛尔（火·远程火铳）/ 卡文（水·辅助治疗）；1s 公共冷却、位置对齐、**资源不重置**（`CharacterRuntime` 为唯一存储）、后台 MP 2%/3s、入场 +10 能量、非激活者关碰撞（技术方案 §3.2/R9）
- **六技能**（`Skills` 执行器 + `CharacterConfig.tres` 数据驱动，倍率按策划案 §9.2）：钢之涟漪 / 钢之洪流（霸体）/ 灼痕印记（火域）/ 苍炎葬列（扇形+点燃）/ 涌泉之环（治疗）/ 深渊之拥（冻结）
- **等价交换 R**：血祭（15% 当前 HP 保底 1 → 伤害 ×1.5 + 吸血 10%）/ 智祭（40% MP → 范围 ×1.5 + CD-30%）/ 质祭（3 炼金尘 → 效果持续 ×2）
- 资源规则：MP 80+2×等级（战斗 2/s、脱战 8/s）、能量 100（普攻+4/技能+8/受击+5/入场+10）、炼金尘（击杀 1-3 掉落）
- 敌人：蚀化猎犬（突进）、锈蚀傀儡（金抗+50%/常驻霸体/砸地）、游荡水元素（风筝+水弹+自挂水）、**精英·矿监工**（霸体条 120 / 破霸 5s 易伤 / 50% 狂暴 / 召唤）
- HUD 战斗件：HP/MP/体力/能量条、E·Q 状态、队伍栏、R 渐晕、炼金尘计数、反应提示

### P2 区域内容·第一轮（commit `d51d231` + 复审 `30da45d`）
- **攀爬**：推向 ≥65° 陡面自动扒墙（法线判定 dot(n,UP)≤cos65°）、墙面四向 2.5m/s、体力 10/s、跳离 / 翻越墙沿 / 耗尽坠落
- **滑翔**：空中再按跳跃展开，垂直钳制 -3m/s、水平可操控、体力 8/s，多条件取消
- **BOSS·蚀核巨像**（level 20 ≈ 10060 HP）：一阶段砸击+冲撞（霸体 200）；二阶段（70%）**金属护壳**（自挂金附着 4GU、金抗+80%、承伤-70%——必须火打熔金直接削壳，3s 输出窗口）；三阶段（30%）**灼热地板** 5/s 灼烧 + 攻速+30% + 承伤+50%，水技能打地板生成**蒸汽安全区**（8s 免灼烧）
- **交互系统**：`Interactable` 基类 + F 键 2.5m 最近对象扫描 + HUD 提示；采集点（铁矿/药草/水镜露）、宝箱（不刷新）、传送阵（解锁 + 环序传送）
- **元素谜题原型**：炼成灯（火附着点亮，复用怪物附着管线）×3 → 密门开启
- **灰烬河谷白模 V2**：250×250 三段结构（矿镇→河谷→洞窟）接入 main；旧竞技场 `p0_arena` 保留作试练场

### P2 区域内容·第二轮（2026-09-11，随开发两轮自查，明细见 `代码审核报告.md` 一·七）
- **元素谜题补齐至 3**：① 炼成灯（火附着点亮）×3 → 密门；② 寒晶镜（水附着点亮）×3 → 密门（灯管线 `target_element` 数据化）；③ 锈蚀锁链门——需在门上触发**锈蚀**反应（先水后金）蚀断锁链，教学反应组合顺序（策划案 §5.3）
- **蚀晶壁**（策划案 §8.2"第一期亮点"）：蚀晶化陡壁徒手不可攀；金元素技能命中生成 5 个抓握点（60s 消失、重复施法去重），踩点攀爬，墙顶设宝箱
- **观景点 ×2**（策划案 §8.1）：攀爬教学墙顶 / 洞窟口，F 观赏弹出景点文本（一次性）
- **采集刷新接存档**（策划案 §8.1 / 技术方案 §7）：`GameSaveFlow` 编排 **F5 存档 / F9 读档**——野外采集读档刷新、洞窟一次性采集与宝箱不刷新（静态注册表持久化）、传送阵解锁、队伍 HP/MP/能量与背包往返
- **密度调优**：洞窟一次性采集 ×2、宝箱 +2（蚀晶壁顶 / 水谜门后）；现采集 17 / 宝箱 10（基线 15/8，偏差已声明待实测后回收或转正）

## 4. 质量与验证状态

- **自动化测试 206 项全绿**（2026-09-11 P2 第二轮交付后）：单元/规则 134（公式锚点、反应表、附着 ICD、状态、Poise、运行时资源、背包、配置与场景可加载性、本地化、池、存档防篡改）+ 端到端集成 72（模拟真实输入：连击/反应管线精确数值/切换语义/BOSS 三阶段与护壳熔金削除双向断言/补刷/滑翔攀爬/蚀晶壁抓握点/锈蚀锁链门/观景点/存档流）
- **冒烟门禁**：主场景 headless 运行扫 `SCRIPT ERROR`，当前 0
- **代码审核**：三轮审核闭环共修复 ~~21~~ **29** 个问题（P0 审核十项、P1 两轮八项、P2 开发期即时六项、P2 深度复审五项），全部有可复现用例锁定。明细见 `代码审核报告.md`（2026-09-11 勘误 E1：原"21"与分项实点不符，见 §11）
- **测试命令**（任何改动后必须全绿才能提交）：
  ```bat
  D:\tools\godot-4.4.1\Godot_v4.4.1-stable_win64_console.exe --headless --path D:\tools\GL res://tests/test_runner.tscn
  D:\tools\godot-4.4.1\Godot_v4.4.1-stable_win64_console.exe --headless --path D:\tools\GL res://tests/integration_runner.tscn
  ```
  （他机按引擎实际安装路径替换。）判读以输出中的 `=== 结果：N/N 通过 ===` 行为准——2026-09-11 本机实测：判定行打印后引擎在退出清理阶段段错误、退出码 139（主/console exe 均复现），属引擎退出问题而非用例失败，见 §11-E5。提交前另跑主场景冒烟（`--quit-after 300`）扫脚本错误。

## 5. 新协作者上手指南

1. **环境**：安装 Godot 4.4.1-stable（勿用其他版本）；克隆本仓库；用 Godot 打开项目目录首次会自动导入。
2. **运行**：直接 F5，或双击 `启动游戏.bat`（路径需按本机改）。
3. **操作**：WASD 移动 / 鼠标视角 / 左键三段连击 / E 炼成技 / Q 炼成爆发 / R 等价交换 / 1·2·3 切角色 / Shift 冲刺 / 空格跳跃·滑翔 / F 交互·观景 / 推墙攀爬（蚀晶壁需金技能造点）/ F5 存档 / F9 读档 / F3 调试 / Esc 暂停。
4. **改数值**：所有手感与战斗参数在 `resources/config/*.tres`（相机/移动/打击感/角色/敌人），不要求改代码。
5. **跑测试**：上方两条命令；改数值公式先跑 `tests/test_runner.tscn`（含策划案锚点断言）。
6. **提交前自查**：全量测试绿 → 主场景冒烟 0 `SCRIPT ERROR` → 更新 `开发记录.md`。

## 6. 架构速览

```
res://
├── scenes/    main(入口) / world(灰烬河谷·竞技场) / characters(player+enemies)
│              common(假人/伤害数字) / combat(水弹) / ui(HUD·暂停·调试)
├── scripts/
│   ├── core/      6 个 Autoload：EventBus(信号总线) GameManager(状态机+顿帧)
│   │              DataManager(只读配置) SaveManager(JSON+校验和)
│   │              AudioManager(骨架) PoolManager(节点池，防悬挂)
│   ├── systems/   DamageFormulas(纯函数) / Combat(结算) / ElementAura(附着)
│   │              ReactionDefinition+Table(反应，数据驱动) / StatusHolder(状态)
│   │              Poise(霸体) / Health / Stamina / GroundField(火域) / 背包
│   ├── player/    PlayerCharacter + PartyManager + CharacterRuntime + Skills
│   │              + IActorSource(联机预留) + states/(idle·run·air·climb·attack·hit)
│   ├── enemies/   EnemyBase + 矿工/猎犬/傀儡/水元素/精英/BOSS
│   ├── world/     p0_arena(试练场) / ash_valley(主场景) / 交互对象×5 / 灼热·蒸汽区
│   ├── config/    相机/移动/打击感/角色×3/敌人×6 配置类
│   └── ui/ debug/ main/
├── resources/  13 个 .tres（只读静态配置——运行时禁止写回，技术方案 §7/C4）
├── data/loc/   zh.csv 本地化（fallback zh_CN，禁止硬编码文案）
└── tests/      两个场景式测试运行器（游戏模式跑，勿用 -s）
```

关键约定：
- **数据分工**：静态配置 = Resource 只读；运行时/存档 = JSON（SaveManager，schema v1 + 校验和）。
- **事件**：跨系统一律走 `EventBus` 信号（击杀事件单一来源在 `EnemyBase._on_died`）。
- **输入**：全部 InputMap 动作名（`IActorSource` 抽象，联机预留），代码零硬编码键位。
- **物理查询**：`intersect_shape` 只在物理步上下文可靠——瞬时近战判定走查询，持续判定（火域）走定时结算。
- **占位资产**：灰模 + 材质名 PLACEHOLDER 规范；特效按元素色重着色复用（`Elements.color`）。

### 已知引擎坑（GDScript 4.4.1 实测踩坑，新代码务必规避）
- 跨行字典字面量的**首键**不能用 `&"..."`（StringName 字面量），会报 "Expected expression as dictionary key"；单行没问题。规避：跨行字典用 String 键（运行时 String/StringName 作字典键可互换）。
- `class_name` **循环引用**（A 引 B、B 引 A）会双双编译失败（Identifier not declared）。规避：低频开关走 `SceneTree` 元数据（如 `gl_show_aura_viz`）或事件解耦。
- `-s script.gd` 模式**不实例化 Autoload**——引用 EventBus 等单例的脚本在该模式无法编译。测试一律用场景运行器（`res://tests/*.tscn` 作启动场景）。
- 对**已释放实例**：赋给类型化变量、`is` 运算都会直接报错，只有 `is_instance_valid()` 安全。
- `intersect_shape` 等空间查询**只在物理步上下文可靠**——测试模拟输入请走 `Input.action_press` + 物理帧，勿在计时器协程里直接调战斗。
- 节点必须先 `add_child` 再调用依赖 `@onready` 的逻辑——先 setup 后入树会让成员初始化跑在树外（P1 曾因此三角色全为艾登）。
- 存档校验和对**文本**做 hash（JSON 往返会把 int 变 float，对解析后结构做 hash 不可靠）。

## 7. 已知问题与挂账（按优先级）

| 类别 | 事项 | 计划 |
|------|------|------|
| 验收 | P2"45-60 分钟不重样"内容密度、"60 FPS"帧率 | P2 后续轮次填充 + 实机测量 |
| 内容 | 洞窟/矿镇美术为灰盒；采集 17 / 宝箱 10 略超策划案基线（15/8，密度调优偏差，实测后回收或转正） | P2 第三轮 |
| 战斗 | 完美闪避（受击前 0.2s 子弹时间）、蓄力重击、玩家受击无敌帧、攀爬中禁止施法 | P1 打磨项，随玩测反馈排期 |
| 战斗口径 | Q 命中返还能量（§9.1 字面 E 专属）；BOSS 无预挂火时护壳削除死路；ICD 实现口径与 §5.3-3 表述差 | 第四轮审查建议级（审核报告一·六），待策划裁定 |
| 资产 | Mixamo 动画 / 音效 / 炼成阵 Shader / 正式 UI 主题全部占位 | 技术方案 §8/§12，P2-P4 |
| 敌人 | 敌人重力/转向参数硬编码；NavigationAgent3D 未接入（平地直线追踪）；仇恨列表缺失 | P1 后续 |
| 工程 | Git LFS 未启用（当前无二进制资产，接入模型/音频前必须启用）；导出自动化未搭 | 接入资产前 |
| 联机 | 仅 `IActorSource` 抽象预留，无任何网络代码（按技术方案 §12 远期） | 远期 |

## 8. 下一步（按序）

1. **P2 第三轮**：地形网格替换灰盒、性能预算实测（对照技术方案 §10.1）、内容密度实测（45-60 分钟）
2. **P3 预研**：13 屏 UI 清单启动前的存档字段映射设计（世界字段已先行接入：采集/宝箱/传送/队伍）

## 9. 协作约定

- **提交信息**：中文、说明"做了什么+为什么"；每轮开发附带自查闭环（检查→修复→再查）并在 `代码审核报告.md` 记录问题清单。
- **文档同步**：改玩法必须对照策划案章节号；新系统必须有对应测试；`开发记录.md` 每轮追加。
- **范围纪律**：新增内容遵守策划案"加一删一"；第一期硬约束 3 角色/3 元素/250m 区域/13 屏 UI 不放宽。
- **git**：仓库级身份已配置（1999MrQ / 1469863345@qq.com）；`.uid` 文件与 `.import` 元数据需提交；`.godot/`、`*.translation`、导出产物不入库。

## 10. 版本历史（git 提交对照）

| 提交 | 内容 |
|------|------|
| `aeda76b` | 初始提交：策划案 / 技术方案 / 审核报告 V2.1（文档基线） |
| `34b7c60` | P0+P1：手感原型 + 战斗核心（含两轮审核修复，当时 161 项测试） |
| `d51d231` | P2 第一轮：攀爬滑翔 / 交互系统 / BOSS 三阶段 / 灰烬河谷 V2（测试扩至 191 项） |
| `30da45d` | P2 深度复审：修复 5 处设计级问题（灼热地板残留 / 边界可攀出图 / 出生点错位 / BOSS 嵌墙 / 宝箱表现） |

> 引擎版本锁定 Godot 4.4.1-stable，升级需全量回归（191 项）后另行提交。

---

## 11. 勘误与修订记录

> 供协作者核对：本节按日期记录对进度报告 / 代码审核报告 / 开发记录三份文档的修正，正文以删除线保留原文，理由逐条给出。

- **2026-09-11 · E1（§4 审核修复计数）**：原文"三轮闭环共修复 21 个问题"。逐条实点 `代码审核报告.md`：P0 审核十项（#1-#10）＋ P1 两轮八项（#P1-1~#P1-8）＋ P2 开发期即时六项（第一部分·二）＋ P2 深度复审五项（R-1~R-5）＝ **29 项**。原"21"与括号内分项任何口径相加均不符（10+8+5=23；计入开发期 6 项为 29），按实点修正。
- **2026-09-11 · E2（"新增用例数"三处不一致）**：`开发记录.md` P1 条目"新增 29 项"、P2 条目"新增 27 项"、`代码审核报告.md` P1 表"新增 26 项"互不符，且均对不上阶段总量差。修正为唯一可核验口径：P1 阶段 111（87+24）→ 161（118+43）＝ **新增 50 项**；P2 阶段 161 → 191（131+60）＝ **新增 30 项**。分项归类的原始口径无存档可查，原文以删除线保留于各处。
- **2026-09-11 · E3（`代码审核报告.md` P1 轮次表自相矛盾）**：表中"第一轮发现 8 个"＋"第二轮发现 3 个真 bug"合计 11，与问题名单 8 条（#P1-1~8）、`开发记录.md`"两轮共修复 8 个"矛盾。按"两轮合计 8、其中第二轮 3 个（含最重的 #P1-3，名单中明确标注为第二轮冒烟抓取）"修正第一轮为 **5 个**；第二轮 3 个中除 #P1-3 外另 2 个已无法从存档辨认归属，不影响总数。
- **2026-09-11 · E4（章节编号与过时引用）**：`代码审核报告.md` 原编号"第一部分（P2）/第三部分（P1）/第四部分（P0）"存在跳号（"第二部分"从未存在），已重排为 第一（P2）/第二（P1）/第三（P0）；`开发记录.md` P1 条目中指向"第一部分"的过时引用同步改为"第二部分"（该文件 P2 条目仍指第一部分，不受影响）。
- **2026-09-11 · E5（环境迁移复验；本条为状态更新而非勘误）**：仓库自旧机 `F:\tools\GL` 迁至 `D:\tools\GL`，本机安装 Godot 4.4.1-stable 至 `D:\tools\godot-4.4.1\`（版本锁定不变）。全量门禁重跑：**单元 131/131、集成 60/60、主场景冒烟 300 帧 0 `SCRIPT ERROR`**——"191 项全绿"在新机复现成立。附发现：三个 headless 进程退出码均为 139，为判定行输出**之后**的引擎退出清理阶段段错误（主/console exe 均复现，伴随已知 RID/ObjectDB 退出泄漏提示），非用例失败，判读口径改为以 `=== 结果 ===` 行为准（§4 已同步）；引擎升级时需回归验证该现象是否消失。`启动游戏.bat` 已改指本机路径。
